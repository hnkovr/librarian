"""librarian — dedupe, categorize, version-stack and catalog course materials.

Mounted as a git submodule at ``src/librarian`` of a host repo (pattern mirrors
``hnkovr/preza_gen``); every scalar lives in ``settings.yml``.
"""

__version__ = "0.1.0"
